# LimeSDR-USB notes

The bundle installs LimeSuite and SoapySDR tools in the Ubuntu 25.04 container.

Useful test commands:

```bash
lsusb
SoapySDRUtil --find
LimeUtil --find
LimeQuickTest
```

The source-build fallback clones `https://github.com/myriadrf/LimeSuite.git`.
