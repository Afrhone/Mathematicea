# Known limitations

- Nomulus is included as a stub / integration placeholder, not as a full turnkey registry deployment.
- UI and SSO stacks are included as templates/presets and still need final secrets and certificate policy.
- Ceph commands must run from a host with admin keyring access.
- Host facts in `directives/hosts.yaml` include placeholders that you must replace with real IPs where noted.
