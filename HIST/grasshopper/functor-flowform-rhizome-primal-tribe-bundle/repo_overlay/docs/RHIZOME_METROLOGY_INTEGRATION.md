# RHIZOME Metrology Integration Overlay

Copy this overlay into `Afrhone/functor-flowform` to add cluster-fidelity gates and metrology-lab automation.

```bash
cp -a repo_overlay/.github ./
cp -a repo_overlay/docs ./
cp -a repo_overlay/cluster ./
```

This overlay does not replace the Flowforms playground. It adds a cluster operations layer.
