stages: [integrity]
integrity:
  stage: integrity
  image: alpine:3.20
  script:
    - apk add --no-cache bash python3
    - bash -n scripts/*.sh scripts/fedora43/*.sh scripts/models/*.sh swarm/*.sh
    - python3 -m json.tool models/manifests/models.json >/dev/null
