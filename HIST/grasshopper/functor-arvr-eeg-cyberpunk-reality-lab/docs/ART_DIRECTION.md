# Λ Direction artistique: Functor Brain Architecture Automation

## 0. Orchestration planning projets en workflow d’automatisation

### a) Low-level basis, axis corpus, modus

```text
basis = signal primitives
axis corpus = form vocabulary
modus = orchestral mechanics
```

The lab starts with no feature and no function:

```text
START 0 features, 0 function
→ build learning curve
→ crash-test
→ run engine functor
```

### b) WEB — I/O

All external services pass through one ingress:

```text
WEB I/O → API Gateway → internal services
```

Social media automation, outpost dispatch, external inference, and future API calls must enter through the gateway.

### c) Crash test and learning curve τ

```text
tau OK  → popularize VR functor system
!tau    → fit learning curve → cycle 0.c
```

## 1. Artistic direction plan scaffolding

```text
VR = environment
AR = flowform
World = form
Modus = mechanics
```

## 2. Multimodal

Inputs:

- EEG
- controller motion
- camera / AR pose
- audio reactive spectrum
- keyboard/mouse
- cluster telemetry
- social/outpost commands

Outputs:

- WebXR scene
- AR flowforms
- simulation sink
- graph interview
- social dispatch
- cluster event stream
