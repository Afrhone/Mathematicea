# Security

- `APPLY=0` by default.
- `ALLOW_CLOUD_CALLS=0` by default.
- `ALLOW_SWAP=0` by default.
- Admin controls require `Authorization: Bearer $ADMIN_TOKEN`.
- Keep ports 7190-7195 on VPN/LAN only.
