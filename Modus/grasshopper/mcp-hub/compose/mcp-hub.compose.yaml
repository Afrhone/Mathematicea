services:
  dashboard:
    image: nginx:alpine
    container_name: niurk-mcp-dashboard
    restart: unless-stopped
    ports:
      - "127.0.0.1:8787:80"
    volumes:
      - ../dashboard/infrasys-webgl:/usr/share/nginx/html:ro
      - ./nginx-dashboard.conf:/etc/nginx/conf.d/default.conf:ro
