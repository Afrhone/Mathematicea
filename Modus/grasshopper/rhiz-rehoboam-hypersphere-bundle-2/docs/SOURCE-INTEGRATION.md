# Source integration notes

Generated from the requested Rehoboam-style UI direction and local RHIZ bundle patterns:

- `lxd-info-system-webgl-bundle`: native WebGL graph + force-layout intent carried forward as the point/edge telemetry surface.
- `kobalt-sigma-llama-gpu-bundle`: llama.cpp/Ollama GPU routing pattern carried into compose profiles.
- `rhiz-compute`: cluster environment concepts, GPU container names, and read-only telemetry orientation.
- `tensorial_phase_bundle`: latent phase/projection vocabulary adapted into the 8D hypersphere surface.
- `rhiz-gpu-ai-model-cluster-bundle`: gateway/MCP/AI-stack shape adapted into the OpenAI-compatible gateway.

This bundle does not copy secrets or mutate cluster state. It provides scaffold code and read-only collector hooks.
