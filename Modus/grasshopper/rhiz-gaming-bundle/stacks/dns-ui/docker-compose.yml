version: "3.9"
services:
  dns-ui:
    image: technitium/dns-server:latest
    ports:
      - "53:53/tcp"
      - "53:53/udp"
      - "5380:5380/tcp"
    volumes:
      - dns_config:/etc/dns
volumes:
  dns_config:
